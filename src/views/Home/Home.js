import React, { Component } from 'react';

const Home = (props) => (
  
    <video src="https://clips.vorwaerts-gmbh.de/VfE_html5.mp4" type="video/mp4" className="anim" onClick={props.clickVideo} autoPlay>
    </video>
  
);

export default Home;
